def alchemy_skill(alchemy_ranks, int_bonus, alchemy_misc):
    alchemy = alchemy_ranks + int_bonus + alchemy_misc
    return alchemy

def appraise_skill(appraise_ranks, int_bonus, appraise_misc):
    appraise = appraise_ranks + int_bonus + appraise_misc
    return appraise

def balance_skill(balance_ranks, dex_bonus, balance_misc):
    balance = balance_ranks + dex_bonus + balance_misc
    return balance

def bluff_skill(bluff_ranks, cha_bonus, bluff_misc):
    bluff = bluff_ranks + cha_bonus + bluff_misc
    return bluff

def butcher_skill(butcher_ranks, str_bonus, butcher_misc):
    butcher = butcher_ranks + str_bonus + butcher_misc
    return butcher

def climb_skill(climb_ranks, str_bonus, climb_misc):
    climb = climb_ranks + str_bonus + climb_misc
    return climb

def concentration_skill(concentration_ranks, con_bonus, concentration_misc):
    concentration = concentration_ranks + con_bonus + concentration_misc
    return concentration

def craft_skill(craft_ranks, int_bonus, craft_misc):
    craft = craft_ranks + int_bonus + craft_misc
    return craft

def decipher_script_skill(decipher_script_ranks, int_bonus, decipher_script_misc):
    decipher_script = decipher_script_ranks + int_bonus + decipher_script_misc
    return decipher_script

def diplomacy_skill(diplomacy_ranks, cha_bonus, diplomacy_misc):
    diplomacy = diplomacy_ranks + cha_bonus + diplomacy_misc
    return diplomacy

def disable_device_skill(disable_device_ranks, int_bonus, disable_device_misc):
    disable_device = disable_device_ranks + int_bonus + disable_device_misc
    return disable_device

def disguise_skill(disguise_ranks, cha_bonus, disguise_misc):
    disguise = disguise_ranks + cha_bonus + disguise_misc
    return disguise

def escape_artist_skill(escape_artist_ranks, dex_bonus, escape_artist_misc):
    escape_artist = escape_artist_ranks + dex_bonus + escape_artist_misc
    return escape_artist

def forge_skill(forge_ranks, str_bonus, forge_misc):
    forge = forge_ranks + str_bonus + forge_misc
    return forge

def forgery_skill(forgery_ranks, int_bonus, forgery_misc):
    forgery = forgery_ranks + int_bonus + forgery_misc
    return forgery

def gather_information_skill(gather_information_ranks, cha_bonus, gather_information_misc):
    gather_information = gather_information_ranks + cha_bonus + gather_information_misc
    return gather_information

def handle_animal_skill(handle_animal_ranks, cha_bonus, handle_animal_misc):
    handle_animal = handle_animal_ranks + cha_bonus + handle_animal_misc
    return handle_animal

def heal_skill(heal_ranks, wis_bonus, heal_misc):
    heal = heal_ranks + wis_bonus + heal_misc
    return heal

def hide_skill(hide_ranks, dex_bonus, hide_misc):
    hide = hide_ranks + dex_bonus + hide_misc
    return hide

def intimidate_skill(intimidate_ranks, cha_bonus, intimidate_misc):
    intimidate = intimidate_ranks + cha_bonus + intimidate_misc
    return intimidate

def jump_skill(jump_ranks, str_bonus, jump_misc):
    jump = jump_ranks + str_bonus + jump_misc
    return jump

def knowledge_skill(knowledge_ranks, int_bonus, knowledge_misc):
    knowledge = knowledge_ranks + int_bonus + knowledge_misc
    return knowledge

def listen_skill(listen_ranks, wis_bonus, listen_misc):
    listen = listen_ranks + wis_bonus + listen_misc
    return listen

def masonry_skill(masonry_ranks, str_bonus, masonry_misc):
    masonry = masonry_ranks + str_bonus + masonry_misc
    return masonry

def mine_skill(mine_ranks, str_bonus, mine_misc):
    mine = mine_ranks + str_bonus + mine_misc
    return mine

def move_silently_skill(move_silently_ranks, dex_bonus, move_silently_misc):
    move_silently = move_silently_ranks + dex_bonus + move_silently_misc
    return move_silently

def open_lock_skill(open_lock_ranks, dex_bonus, open_lock_misc):
    open_lock = open_lock_ranks + dex_bonus + open_lock_misc
    return open_lock

def perform_skill(perform_ranks, cha_bonus, perform_misc):
    perform = perform_ranks + cha_bonus + perform_misc
    return perform

def pilot_skill(pilot_ranks, int_bonus, pilot_misc):
    pilot = pilot_ranks + int_bonus + pilot_misc
    return pilot

def profession_skill(profession_ranks, wis_bonus, profession_misc):
    profession = profession_ranks + wis_bonus + profession_misc
    return profession

def ride_skill(ride_ranks, dex_bonus, ride_misc):
    ride = ride_ranks + dex_bonus + ride_misc
    return ride

def search_skill(search_ranks, int_bonus, search_misc):
    search = search_ranks + int_bonus + search_misc
    return search

def sense_motive_skill(sense_motive_ranks, wis_bonus, sense_motive_misc):
    sense_motive = sense_motive_ranks + wis_bonus + sense_motive_misc
    return sense_motive

def sleight_of_hand_skill(sleight_of_hand_ranks, dex_bonus, sleight_of_hand_misc):
    sleight_of_hand = sleight_of_hand_ranks + dex_bonus + sleight_of_hand_misc
    return sleight_of_hand

def smelt_skill(smelt_ranks, int_bonus, smelt_misc):
    smelt = smelt_ranks + int_bonus + smelt_misc
    return smelt

def speak_languages_skill(speak_languages_ranks, int_bonus, speak_languages_misc):
    speak_languages = speak_languages_ranks + int_bonus + speak_languages_misc
    return speak_languages

def spellcraft_skill(spellcraft_ranks, int_bonus, spellcraft_misc):
    spellcraft = spellcraft_ranks + int_bonus + spellcraft_misc
    return spellcraft

def spot_skill(spot_ranks, wis_bonus, spot_misc):
    spot = spot_ranks + wis_bonus + spot_misc
    return spot

def surgery_skill(surgery_ranks, int_bonus, surgery_misc):
    surgery = surgery_ranks + int_bonus + surgery_misc
    return surgery

def survival_skill(survival_ranks, wis_bonus, survival_misc):
    survival = survival_ranks + wis_bonus + survival_misc
    return survival

def swim_skill(swim_ranks, str_bonus, swim_misc):
    swim = swim_ranks + str_bonus + swim_misc
    return swim

def tumble_skill(tumble_ranks, dex_bonus, tumble_misc):
    tumble = tumble_ranks + dex_bonus + tumble_misc
    return tumble

def use_magic_device_skill(use_magic_device_ranks, cha_bonus, use_magic_device_misc):
    use_magic_device = use_magic_device_ranks + cha_bonus + use_magic_device_misc
    return use_magic_device

def use_rope_skill(use_rope_ranks, dex_bonus, use_rope_misc):
    use_rope = use_rope_ranks + dex_bonus + use_rope_misc
    return use_rope